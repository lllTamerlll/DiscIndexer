<Page
    x:Class="Cost_Calculation.Pages.AgentsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="16,12,16,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="lblTitle" Grid.Column="0"
                       FontSize="14" FontWeight="Bold"
                       Foreground="{StaticResource BrushTextPrimary}"
                       VerticalAlignment="Center"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="10"
                        VerticalAlignment="Center">
                <TextBox x:Name="searchBox"
                         Width="200"
                         PlaceholderText="Поиск в аккаунте…"
                         TextChanged="SearchBox_TextChanged"/>
                <Button x:Name="btnAdd"
                        Content="➕  Добавить агентов"
                        Click="BtnAdd_Click"
                        Background="{StaticResource BrushSurface}"
                        Foreground="{StaticResource BrushAccent}"
                        FontWeight="SemiBold"/>
            </StackPanel>
        </Grid>

        <ScrollViewer Grid.Row="1"
                      HorizontalScrollBarVisibility="Disabled"
                      HorizontalScrollMode="Disabled"
                      VerticalScrollBarVisibility="Auto"
                      VerticalScrollMode="Auto"
                      Margin="8,0,8,8">
            <ItemsRepeater x:Name="agentsRepeater" Margin="8">
                <ItemsRepeater.Layout>
                    <UniformGridLayout MinRowSpacing="14" MinColumnSpacing="14"
                                       ItemsStretch="None"/>
                </ItemsRepeater.Layout>
            </ItemsRepeater>
        </ScrollViewer>

        <StackPanel x:Name="emptyState"
                    Grid.Row="1"
                    Visibility="Collapsed"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Spacing="10">
            <TextBlock x:Name="emptyIcon" Text="🧑‍🚀" FontSize="44"
                       HorizontalAlignment="Center"/>
            <TextBlock x:Name="emptyTitle" Text="В аккаунте пока нет агентов"
                       FontSize="16" FontWeight="Bold"
                       Foreground="{StaticResource BrushTextPrimary}"
                       HorizontalAlignment="Center"/>
            <TextBlock x:Name="emptySub" Text="Нажмите «Добавить агентов», чтобы собрать ростер"
                       FontSize="13"
                       Foreground="{StaticResource BrushTextSecondary}"
                       HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Page>
